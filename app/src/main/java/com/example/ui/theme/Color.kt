package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Warm, Earthy Color Palette (Consistency over Perfection)
val EarthPrimary = Color(0xFF4C754B)        // Warm Mossy Green for "Done"
val EarthBackgroundLight = Color(0xFFFBF8F3) // Warm Linen/Paper Cream Background
val EarthCardLight = Color(0xFFF4EDE2)       // Light Sandy/Beige Flat cards

val EarthAmber = Color(0xFFD48A37)           // Warm Sunny Amber for "Skipped"

val EarthGrayQuiet = Color(0xFFBDC5BD)       // Muted Minty Sage-Gray for "Missed"
val EarthDarkText = Color(0xFF2E382E)        // Charcoal Dark Sage for readable text
val EarthLightAccent = Color(0xFFE5DDD0)     // Border/Line highlight

// Dark Mode Equivalents
val EarthPrimaryDark = Color(0xFF8CAF88)     // Soft Muted Sage for primary
val EarthBackgroundDark = Color(0xFF1B231B)  // Dark Velvet Forest Background
val EarthCardDark = Color(0xFF242E24)        // Rich Dark Juniper card background
val EarthAmberDark = Color(0xFFECA34E)       // Bright Peach Amber for dark mode skips
val EarthGrayQuietDark = Color(0xFF4C584C)   // Shadowy Charcoal Green for missed
val EarthLightText = Color(0xFFEDE9E5)       // Soft Creamy White for text
